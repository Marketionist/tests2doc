'use strict';
/* global describe, it, expect, browser */

// #############################################################################
// INTEGRATION TEST

describe('CRM integration tests: ', function () {

    it('log in to the site with valid username and password', function () {
    });

    it('create a new lead, validate its status', () => {
    });

    // eslint-disable-next-line space-before-function-paren
    it('change the name of the "New" status to "Updated", validate it changed inside lead', function() {
    });

    it('change the name of the "Old" status to "Updated", validate it changed inside lead', ()=>{
    });

    it('change back the name of the status to "New"', function (parameter) {
    });

    it('change back the name of the status to "Old"', (parameter) => {
    });

});
